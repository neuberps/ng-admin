import { ServicessearchfilterPipe } from './servicessearchfilter.pipe';

describe('ServicessearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicessearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
